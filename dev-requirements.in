# Test requirements.

# TODO: Switch to unittest.mock after we drop support for Python 2.
mock~=1.3

# Bound more-itertools so tests can still run on Python 2.
more-itertools<6

# TODO: Upgrade moto someday, maybe when we drop boto2.
moto~=0.4
pytest~=4.6
pytest-cov~=2.10


# Tools for development or for running tests.
cfgv==2.0.1
detect-secrets==0.13.0
pre-commit==1.21.0
tox==3.13.1
